<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.herasymc.cmput301counter"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.herasymc.cmput301counter.CounterListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.herasymc.cmput301counter.CounterViewActivity"
            android:label="@string/title_activity_counter_view"
            android:parentActivityName="com.herasymc.cmput301counter.CounterListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.herasymc.cmput301counter.CounterListActivity" />
        </activity>
        <activity
            android:name="com.herasymc.cmput301counter.CounterSummaryActivity"
            android:label="@string/title_activity_counter_summary"
            android:parentActivityName="com.herasymc.cmput301counter.CounterListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.herasymc.cmput301counter.CounterListActivity" />
        </activity>
    </application>

</manifest>
